# ============================================================================
# AI LISTER - ENVIRONMENT CONFIGURATION
# ============================================================================
# This file contains ONLY app-wide settings.
# User-specific credentials (marketplace logins) are stored in the database.
# ============================================================================

# ============================================================================
# AI API KEYS
# ============================================================================

# OpenAI API Key (for GPT-4 Vision photo analysis)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key

# Anthropic API Key (for Claude listing enhancement)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Google AI (Gemini) API Key (for fast item classification - PRIMARY ANALYZER)
# Get from: https://aistudio.google.com/app/apikey
# This is used for the main "Analyze with AI" button (fast, cost-effective classification)
# Gemini detects: item type, brand, category, basic description
GEMINI_API_KEY=your-google-ai-api-key

# Alternative env var names (any of these work):
# GOOGLE_AI_API_KEY=your-google-ai-api-key
# GEMENI_API_KEY=your-google-ai-api-key  (common typo - also supported)

# ============================================================================
# AI MODEL CONFIGURATION (Optional)
# ============================================================================

# Gemini Model (defaults to gemini-2.5-flash)
# Available image-capable models:
# - gemini-2.5-flash (DEFAULT - fastest, cheapest, great for classification)
# - gemini-2.5-pro (better quality, more expensive)
# - gemini-2.0-flash (older but still supported)
GEMINI_MODEL=gemini-2.5-flash

# Claude Model (defaults to claude-3-haiku-20240307)
# Used for listing creation (attribute detection, descriptions)
# Available models (from fastest/cheapest to slowest/most expensive):
# - claude-3-haiku-20240307 (DEFAULT - fast, cheap, works with all API tiers)
# - claude-3-sonnet-20240229 (better quality, more expensive)
# - claude-3-opus-20240229 (best quality, most expensive)
CLAUDE_MODEL=claude-3-haiku-20240307

# Claude Model for Collectible Identification (defaults to claude-3-haiku-20240307)
CLAUDE_COLLECTIBLE_MODEL=claude-3-haiku-20240307

# ============================================================================
# APP FLAGS
# ============================================================================

# Automatically enhance listings with AI
AUTO_ENHANCE=true

# Use sandbox mode for testing (prevents real posts)
USE_SANDBOX=false

# Automatically process photos from designated folder
AUTO_PROCESS_PHOTOS=false

# ============================================================================
# NOTIFICATION EMAIL (Central SMTP Account)
# ============================================================================
# These credentials are used by the APP to send notification emails to users.
# Users configure where they want to RECEIVE notifications in their settings.
# Users do NOT need to provide SMTP credentials.
#
# SMTP Configuration - Use your email provider's SMTP settings
# Gmail: smtp.gmail.com (port 587) - requires app password
# Outlook: smtp.office365.com (port 587)
# Yahoo: smtp.mail.yahoo.com (port 587 or 465)
# ============================================================================

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=webapp.notification.email@gmail.com
SMTP_PASSWORD=your_gmail_app_password

# ============================================================================
# FLASK SECRET KEY (Required for Production)
# ============================================================================
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
# IMPORTANT: Change this in production!
# ============================================================================

FLASK_SECRET_KEY=dev-secret-key-change-in-production

# ============================================================================
# MARKETPLACE CREDENTIALS - REMOVED
# ============================================================================
# Marketplace credentials (eBay, Mercari, Poshmark, Depop, etc.) are NO LONGER
# stored in .env. They are stored PER USER in the database for security and
# multi-user support.
#
# Users manage their marketplace logins in the Settings page after registering.
# ============================================================================
