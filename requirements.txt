# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0

# Browser automation for Mercari (if not using Shops API)
playwright>=1.40.0

# Data validation
pydantic>=2.0.0

# Console output formatting
rich>=13.0.0

# Optional: Image processing
Pillow>=10.0.0
rembg>=2.0.50  # Background removal (free, open-source)

# GUI
customtkinter>=5.2.0

# Web app
flask>=3.0.0
flask-login>=0.6.3
flask-session>=0.6.0  # Server-side session storage for multi-worker support
cachelib>=0.10.0  # Required by flask-session
werkzeug>=3.0.0
gunicorn>=21.2.0  # Production WSGI server for Render deployment
gevent>=23.9.1  # Async worker for better upload handling

# Database
psycopg2-binary>=2.9.9  # PostgreSQL adapter

# Authentication (Supabase OAuth)
supabase>=2.3.0  # Supabase Python client
httpx>=0.25.0  # HTTP client for OAuth token exchange
pyjwt>=2.8.0  # JWT token handling
passlib[bcrypt]>=1.7.4  # Password hashing
python-jose[cryptography]>=3.3.0  # JWT creation and validation

# Cloud Storage
cloudinary>=1.41.0  # Photo storage (FREE tier: 25GB)

# PDF Generation
reportlab>=4.0.0  # PDF generation for packing slips, labels, invoices

# Billing & Payments
stripe>=7.0.0  # Stripe integration for subscriptions

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
flask-testing>=0.8.0
